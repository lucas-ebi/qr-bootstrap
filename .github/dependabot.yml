# Keeps the GitHub Actions used by the workflows up to date (the project has no npm dependencies).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
